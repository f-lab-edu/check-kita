export enum BookRecordType {
  already = 'already',
  ing = 'ing',
  want = 'want',
}

export enum BookRecordTypeLabel {
  already = '읽은 책',
  ing = '읽고 있는 책',
  want = '읽고 싶은 책',
}

export enum BookReadingProgressType {
  pages = 'pages',
  percentage = 'percentage',
}
